# Development Setup

Node.js 22+ and PNPM required for development

```bash
git submodule update --init
pnpm i
pnpm build:dreamland
pnpm build (x2)
pnpm dev
```

# Incremental Build

```bash
pnpm dev
```
